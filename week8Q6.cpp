#include <iostream>
#include <string>

using namespace std;

string text;

int i, length;


int main(){
  cout << "Type in a line of text, then press enter: ";
  getline(cin, text);
  length = length(text);
  i = 0;
  while (i < length){

    i++;
  }
}
